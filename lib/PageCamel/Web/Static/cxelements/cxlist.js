class CXList extends CXBox {
    constructor(ctx, x, y, width, height) {
        super(ctx, x, y, width, height);
    }
    draw() {
        this._drawBox();
        this._drawList();
    }
    _drawList() {

    }
    setList() {
        
    }
}